# Discord Link Demo

Files added:
- `index.html` — simple page matching your style
- `styles.css` — styling and responsive adjustments
- `backgrounds/night-sky.svg` — placeholder background that approximates your screenshot

Usage:
- Open `index.html` in a browser. Click the "click here" link to open https://discord.com in a new tab.
- Replace `backgrounds/night-sky.svg` with your own high-resolution background image if you prefer (use same filename or update `styles.css`).

If you'd like, I can: 
- Add more decorative animations (glow, floating stars)
- Replace the SVG with a large JPG/PNG you provide
- Make the layout content editable via variables
